export enum CliCommands {
  Path = "path",
  Scan = "scan",
  Configure = "configure",
  Auth = "auth",
}

export enum CommandParameters {
  ScanInfoFormatJson = "--output=json",
  Usage = "--help",
}
