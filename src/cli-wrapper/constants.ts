export enum CliCommands {
  Path = "path",
  Scan = "scan",
  Configure = "configure",
  Auth = "auth",
  Ignore = "ignore",
}

export enum CommandParameters {
  OutputFormatJson = "--output=json",
  Usage = "--help",
  ByRule = "--by-rule",
}
